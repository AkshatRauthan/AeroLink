/**
 * Single source of truth for every Kafka topic name.
 */
export const Topics = {
    flightDataGenerated: 'flight.data.generated', // Data Gen Service -> Flight Service
    priceUpdated: 'price.updated',
};